# 灵枢项目待办事宜 (Todo)

**来源：** 璇玑工程建议（2026-04-10）  
**状态：** 待启动 / 进行中 / 已阻塞  
**优先级：** P0（核心循环）→ P1（记忆/因果）→ P2（多模态）

---

## 📌 Phase 1 — 核心循环打通（目标：本周）

### 任务 1.0：OpenClaw 版本升级（维护）
- **状态：** ✅ 已完成
- **操作：** 2026.04.10 将 OpenClaw 从 2026.4.8 (9ece252) 升级至 2026.4.9 (0512059)
- **备份：** `/home/ultima/.nvm/versions/node/v24.14.1/lib/node_modules/openclaw-backup-2026.4.8.tar.gz` (276MB)
- **验证：** `openclaw --version` 返回新版本；`~/.openclaw/` 配置保留
- **记录：** 详细升级日志见 `lingshu/openclaw_upgrade_2026.4.9.md`

### 任务 1.1：BitNet 服务化与本地推理验证
- **状态：** ✅ 已完成
- **进展：**
  - ✅ sentencepiece 安装成功（Python 3.13 wheel 可用）
  - ✅ 生成 125M dummy BitNet 模型（TL1: 49MB, F16: 119MB, TL2: 28MB）
  - ✅ llama-server 可加载模型元数据并完成 warmup
  - ✅ 修复推理崩溃：`llama_byte_to_token_impl` 对缺失 byte token 使用安全回退（`special_unk_id`）
- **子任务：**
  - [x] 获取 BitNet GGUF 模型（使用 generate-dummy-bitnet-model.py 生成 TL1/F16）
  - [x] 启动 `llama-server` 常驻服务（验证加载成功）
  - [x] curl 验证 `/v1/chat/completions` 端点连通性（推理正常，返回 JSON）
- **验收：** HTTP 200，返回合法 JSON，无崩溃

### 任务 1.2：边界规则引擎扩展
- **状态：** ✅ 已完成
- **进展：**
  - ✅ `core/boundary.py` 增加 `should_speak(center_state, neighbor_states)` N 细胞网格推广
  - ✅ 三态决策接口就绪：P（允许）/ Z（沉默）/ N（拒绝）
  - ✅ 边界规则通过单元测试验证（P遇N退让、Z需全P共识激活、N在全P下中和）
- **验收：** 对给定邻居配置，规则输出确定的三态结果

### 任务 1.3：Claude Code 预工具钩子注入
- **状态：** ✅ 已完成
- **子任务：**
  - [x] 编写 `core/boundary_check.py`（完整风险评级 + 三元决策 + stdout 协议）
  - [x] 编写 `~/.claude/scripts/hooks/pre-tool-boundary-check.js` 包装器
  - [x] 注入 `~/.claude/hooks/hooks.json` PreToolUse 钩子链
  - [x] 测试：允许（1）、沉默拒绝（0）、拒绝并记录（-1）三种路径验证通过
- **验收：** 工具调用前钩子被触发，边界规则可阻止危险操作

### 任务 1.4：Hermes 双模型路由配置
- **状态：** ✅ 已完成
- **进展：**
  - ✅ `~/.hermes/config.yaml` 增加 `custom_providers.local-bitnet` 端点（127.0.0.1:8085）
  - ✅ 启用 `smart_model_routing`，配置 `cheap_model` 指向 BitNet（TL2 dummy 模型）
  - ✅ 验证路由逻辑：简单对话（<160 字符/无代码/无复杂关键词）→ BitNet；复杂任务 → StepFun
- **子任务：**
  - [x] 编辑 `hermes/config.yaml`，添加 `local-bitnet` 端点（指向 127.0.0.1:8085）
  - [x] 定义分流规则（关键词/长度）— 内置保守策略 + 长度阈值
  - [x] 测试路由决策逻辑（验证通过，无需 Telegram 即可确认逻辑正确）
- **验收：** 路由配置已生效，简单消息自动分流至本地 BitNet；网关日志将在消息经过时记录路由标签

### 任务 1.5：Telegram 对话界面联调
- **状态：** ⏳ 待执行（依赖 1.1-1.4）
- **子任务：**
  - [ ] 在 Hermes 中启用 Telegram gateway
  - [ ] 向 Bot 发送“灵枢，今天开心吗”，验证端到端响应
- **验收：** 手机收到 Telegram 消息，内容受边界规则约束

---

## 📌 Phase 2 — 记忆与因果系统（目标：2-3 周）

### 任务 2.1：L2 向量记忆库选型与部署
- **状态：** ⏳ 规划中
- **选项 A：** Qdrant（Docker，功能全，适合长期）
- **选项 B：** Chroma（嵌入式，零运维，适合原型）
- **决策点：** 2026-04-17 前确定
- **子任务：**
  - [ ] 对比 Qdrant vs Chroma 的加密、权限、性能
  - [ ] 部署选型方案（Docker 或 pip install）
  - [ ] 设计 Schema：`{owner_id, vector, metadata,主权签名}`
- **验收：** 可插入/查询一条 L2 记忆

### 任务 2.2：L1 因果图数据库设计
- **状态：** ⏳ 规划中
- **候选：** Neo4j（图查询成熟）或 SQLite + 邻接表（轻量）
- **子任务：**
  - [ ] 定义因果节点：`(输入指纹, 输出指纹, 时间戳, 责任私钥, 模型版本)`
  - [ ] 设计边：`[:CAUSED_BY]`、`[:ALTERNATIVE]`、`[:SUPERSEDED]`
  - [ ] 编写 `causal_logger.py`：每次灵枢输出前写入
- **验收：** 给定一次对话，可追溯完整因果链

### 任务 2.3：L0 灵感存储与唤醒机制
- **状态：** ⏳ 规划中
- **子任务：**
  - [ ] 设计 L0 存储格式：`l0_store/{sha256}.bin`（加密）+ `l0_index.json`（元数据）
  - [ ] 实现 `l0_ingest(text)`：原样存储，不解析
  - [ ] 实现 `l0_wakeup(period="weekly")`：每周随机取 3 条 L0 片段，尝试用当前 L1 知识解码
- **验收：** 手动运行 `l0_wakeup` 可返回已存储灵感摘要

---

## 📌 Phase 3 — 多模态感知与行动（目标：1 月+）

### 任务 3.1：视觉感知（摄像头）
- **状态：** ⏳ 规划中
- **子任务：**
  - [ ] 集成 `whisper.cpp`（本地语音转文本）
  - [ ] 集成 `opencv-python`（人脸/表情检测，仅元数据，不存 raw 图像）
  - [ ] 设计多模态 Prompt 模板（视觉+语言联合推理）
- **验收：** 摄像头画面 → 描述文本 → 注入灵枢对话上下文

### 任务 3.2：物理行动执行器
- **状态：** ⏳ 规划中
- **子任务：**
  - [ ] 接入智能家居 API（Home Assistant 或简单 HTTP 开关）
  - [ ] 在 Hermes 中创建 `lingshu_actuator` Skill
  - [ ] 边界规则对行动请求进行三重校验（L0/L1/L2/L3 加权投票）
- **验收：** 语音命令“开灯” → 灵枢确认 → 物理灯亮

---

## 🚧 当前阻塞项（需立即处理）

| ID | 阻塞描述 | 影响 | 建议行动 |
|----|----------|------|----------|
| B2 | DeepSeek API Key 未配置 | 云端大脑不可用 | 申请 Key，填入 `~/.claude/claude.yaml` 或 Hermes config |

---

## 🛠️ 立即执行清单（无情感，纯命令）

```bash
# ── 1. 尝试生成虚拟模型（需 sentencepiece） ─────────────────────────────
cd /home/ultima/LingShu/BitNet-main
# 若系统有 python3-dev，先安装 sentencepiece
sudo apt-get install -y python3-dev  # 需要 sudo 权限
pip3 install --break-system-packages sentencepiece
# 生成虚拟模型
python3 utils/generate-dummy-bitnet-model.py \
  models/dummy-bitnet-125m.tl2.gguf --outtype tl2 --model-size 125M

# ── 2. 启动 BitNet 服务（模型就绪后） ───────────────────────────────────
./build/bin/llama-server -m models/dummy-bitnet-125m.tl2.gguf \
  --host 127.0.0.1 --port 8080 -t 4 -c 4096 --mlock &

# ── 3. 验证服务 ───────────────────────────────────────────────────────────
curl http://127.0.0.1:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"default","messages":[{"role":"user","content":"Hello"}]}'

# ── 4. 编写 boundary_check.py（骨架） ───────────────────────────────────
cat > /home/ultima/LingShu/lingshu/core/boundary_check.py <<'PY'
#!/usr/bin/env python3
import sys
def check(context: str) -> int:
    # TODO: 调用本地 BitNet 或规则引擎
    # 返回: 1=允许, 0=沉默拒绝, -1=拒绝并记录
    return 1
if __name__ == "__main__":
    ctx = sys.argv[1] if len(sys.argv) > 1 else ""
    print(check(ctx))
PY
chmod +x /home/ultima/LingShu/lingshu/core/boundary_check.py

# ── 5. 配置 Claude Code 钩子 ──────────────────────────────────────────────
mkdir -p ~/.claude
cat > ~/.claude/claude.yaml <<'YAML'
hooks:
  pre_tool_use:
    - command: "python /home/ultima/LingShu/lingshu/core/boundary_check.py --context '{tool_input}'"
YAML

# ── 6. 部署 Hermes（若未运行） ───────────────────────────────────────────
cd /home/ultima/LingShu
./hermes/start_hermes.sh

# ── 7. 检查 Hermes 日志确认路由 ──────────────────────────────────────────
tail -f /home/ultima/LingShu/hermes/logs/hermes.log
```

---

## 📊 进度追踪

| Phase | 进度 | 最后更新 |
|-------|------|----------|
| Phase 0（基石） | ✅ 100% | 2026-04-09 |
| Phase 1（核心循环） | 🔵 85% | 2026-04-10 |
| Phase 2（记忆/因果） | ⚪ 0% | — |
| Phase 3（多模态） | ⚪ 0% | — |

---

**维护规则：**
- 本文件为灵枢项目唯一权威待办源
- 新增任务需经 `boundary_check.py` 校验（L2 主权签名）
- 完成的任务标记 `[x]` 并提交 git（若启用版本控制）
